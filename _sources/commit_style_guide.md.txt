# Руководство по написанию коммитов

Хорошо написанные коммиты — это ключ к понятной истории изменений проекта.  
Они упрощают отладку, ревью, мёрж и автоматическую генерацию changelog'ов.

---

## 🎯 Зачем нужны хорошие коммиты?

- Упрощают понимание истории изменений
- Помогают при отладке (`git bisect`)
- Удобны для автоматической генерации `CHANGELOG.md`
- Облегчают работу всей команды

---

## 🔤 Формат сообщения коммита

```
<type>: <краткое описание>

Причина изменения:
Что было изменено:
Как это реализовано:
Затронутые компоненты:
Ссылки:
Breaking changes:
```

### Типы коммитов

| Тип      | Описание |
|----------|----------|
| `feat`   | Новая фича |
| `fix`    | Исправление бага |
| `chore`  | Небольшие правки (обновление зависимостей, форматирование) |
| `docs`   | Изменения в документации |
| `style`  | Изменения стиля кода (отступы, форматирование) |
| `refactor` | Рефакторинг без изменения поведения |
| `perf`   | Оптимизация производительности |
| `test`   | Добавление или исправление тестов |
| `build`  | Сборка или внешние зависимости |
| `ci`     | CI/CD конфигурации |
| `revert` | Откат предыдущего коммита |

---

### Примеры хороших коммитов

```text
feat: добавить авторизацию через Google
```

```text
fix: обработать ошибку при отсутствии токена в заголовках

В некоторых случаях запросы без Authorization заголовка приводили к Internal Server Error.
Теперь возвращается 401 Unauthorized.
```

```text
perf: оптимизировать SQL-запросы в модуле пользователей

Уменьшено количество обращений к БД с 15 до 3 на странице профиля.
```

```text
docs: обновить README.md с новыми инструкциями по запуску
```

```text
test: добавить unit-тесты для utils.py
```

```text
ci: добавить workflow для проверки типов в GitHub Actions
```

---

### Плохие примеры (не делай так)

```text
update
```

```text
some changes
```

```text
fixed bug
```

```text
wip
```

```text
merge branch
```

---

## 💡 Полезные практики

- Пиши коммиты до `push`, а не после
- Правь их перед пу́шем: `git commit --amend`
- Используй `git rebase -i` для чистой истории
- Поддерживай читаемость даже если ты один
- Делай подробное описание, если изменение нетривиальное


## ✅ Чек-лист: хороший ли твой коммит?

- ✅ Есть ли префикс типа (`feat`, `fix`, `docs`)?
- ✅ Краткое описание говорит, что было сделано?
- ✅ Подробное описание объясняет *почему*, если это не очевидно?
- ✅ Сообщение не содержит лишних деталей или мусора?
- ✅ Если это крупное изменение — есть ли ссылка на задачу или issue?