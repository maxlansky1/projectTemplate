# `pre-commit`

`pre-commit` — это фреймворк для управления и запуска "хуков" (hook), которые выполняют проверки или преобразования кода *перед* тем, как вы его закоммитите в репозиторий Git. Это помогает автоматически обеспечивать единообразие кода, выявлять потенциальные проблемы и предотвращать попадание в репозиторий нежелательного содержимого (например, конфиденциальных данных или слишком больших файлов).

## ⚙️ Настройки pre-commit

### 🕵️‍♂️ .pre-commit-config.yaml

Файл `.pre-commit-config.yaml` определяет, какие репозитории с хуками использовать и какие конкретно хуки из них запускать. Он указывает источники (`repo`), версии (`rev`) и конфигурации (`hooks`) для каждого инструмента.

```{eval-rst}
.. toggle:: Показать

   .. literalinclude:: ../../.pre-commit-config.yaml
      :language: yaml
      :caption: .pre-commit-config.yaml
```

### ruff (Линтинг и форматирование)

Ruff — это быстрый инструмент линтинга и форматирования Python-кода, написанный на Rust.

- **`ruff-format`**: Форматирует Python-файлы в соответствии с настройками Ruff (обычно соответствует стилю Black). Автоматически применяет изменения к файлам.
- **`ruff-check`**: Проверяет Python-файлы на наличие стилистических ошибок, потенциальных багов и несоответствий PEP 8. Аргумент `--fix` позволяет автоматически исправить большинство обнаруженных проблем. Аргумент `--exit-non-zero-on-fix` заставляет хук завершаться с ненулевым кодом возврата, если были внесены какие-либо исправления, что может быть полезно для CI/CD или для принудительного выполнения `git add` после фиксации.

### pre-commit-hooks (Базовые проверки)

Этот репозиторий содержит набор универсальных хуков для общих проверок.

- **`check-added-large-files`**: Проверяет, не добавлены ли в коммит слишком большие файлы (по умолчанию > 500 кБ). Это помогает избежать случайного добавления бинарных файлов, логов или артефактов сборки в репозиторий.
- **`check-case-conflict`**: Проверяет файлы, которые могут конфликтовать в файловых системах, чувствительных к регистру (например, Linux) и нечувствительных к регистру (например, Windows, macOS).
- **`check-illegal-windows-names`**: Проверяет файлы с именами, которые недопустимы в Windows (например, `CON`, `PRN`, `AUX`, `NUL` и т.д.).
- **`name-tests-test`**: Проверяет, что файлы тестов имеют корректные имена (например, `*_test.py`, `test_*.py`).
- **`detect-private-key`**: Проверяет, не были ли случайно добавлены в коммит файлы с приватными ключами (например, `.pem`, `.key`, `.p12`).
- **`check-docstring-first`**: Проверяет, что docstring находится в самом начале файла, до любого другого кода.
- **`check-symlinks`**: Проверяет символические ссылки, которые не указывают никуда (сломанные).
- **`check-json`**: Проверяет, что файлы JSON имеют корректный синтаксис.
- **`check-toml`**: Проверяет, что файлы TOML имеют корректный синтаксис.
- **`check-yaml`**: Проверяет, что файлы YAML имеют корректный синтаксис.

### detect-secrets (Поиск секретов)

Инструмент для обнаружения потенциальных секретов (паролей, токенов, ключей API) в файлах, добавленных в Git.

- **`detect-secrets`**: Сканирует файлы на наличие строк, похожих на секреты, используя различные детекторы (например, для API-ключей AWS, паролей, приватных ключей). Рекомендуется использовать в сочетании с `.secrets.baseline` файлом для исключения ложных срабатываний или фиктивных данных.

### pyupgrade (Синтаксис)

Инструмент для автоматического обновления синтаксиса Python-кода до более новых версий языка.

- **`pyupgrade`**: Обновляет синтаксис, например, заменяет `dict((a, b) for a, b in y)` на `{a: b for a, b in y}`, удаляет устаревшие импорты `__future__`, преобразует `str()` в `""`, обновляет синтаксис `super(ClassName, self)` до `super()`, и т.д., в зависимости от целевой версии Python, указанной в конфигурации проекта.

### isort (Сортировка импортов)

Инструмент для сортировки и форматирования импортов в Python-файлах.

- **`isort`**: Сортирует импорты по алфавиту и группирует их (стандартная библиотека, внешние пакеты, локальные импорты). Аргументы `--profile black` и `--filter-files` указывают использовать стиль форматирования, совместимый с Black, и применять изменения только к файлам, которые уже отформатированы, соответственно.

### interrogate (Покрытие docstrings)

Инструмент для проверки наличия и качества docstring'ов в Python-коде.

- **`interrogate`**: Проверяет, есть ли docstring'и у модулей, классов, методов и функций. Аргументы:
  - `--fail-under=80`: Проверка завершится с ошибкой, если процент покрытия docstring'ами будет ниже 80%.
  - `-vv`: Включает подробный вывод.
  - `-i`, `-I`: Игнорировать `__init__.py` файлы и пустые `__init__.py`.
  - `-m`: Проверять модули на наличие docstring'а.

### bandit (Безопасность)

Статический анализатор безопасности для Python-кода.

- **`bandit`**: Сканирует исходный код на наличие распространённых уязвимостей и проблем безопасности, таких как использование небезопасных функций (например, `eval`, `exec`), неправильная обработка ввода, утечки секретов и т.д.

## 🔗 Полезные ссылки

- [Официальная документация pre-commit](https://pre-commit.com/)
- [Ruff](https://docs.astral.sh/ruff/)
- [pre-commit-hooks](https://github.com/pre-commit/pre-commit-hooks)
- [detect-secrets](https://github.com/Yelp/detect-secrets)
- [pyupgrade](https://github.com/asottile/pyupgrade)
- [isort](https://pycqa.github.io/isort/)
- [interrogate](https://interrogate.readthedocs.io/)
- [Bandit](https://bandit.readthedocs.io/)