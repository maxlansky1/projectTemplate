# `copier`

## 🧩 Что такое copier

copier — это инструмент командной строки для копирования и обновления проектных шаблонов. Он позволяет создавать шаблоны проектов с параметрами (вопросами для пользователя) и поддерживать их актуальность, обновляя уже скопированные проекты при изменении шаблона. copier использует шаблоны Jinja2 для генерации файлов и может управлять сложными сценариями, включая выполнение пользовательских скриптов после копирования.

## 📐 Настройки Copier

### ⚙️ copier.yaml

Файл `copier.yaml` (или `copier.yml`) является центральным конфигурационным файлом шаблона проекта для copier. Он определяет структуру шаблона, вопросы, задаваемые пользователю, и поведение самого процесса копирования.

**Основные функции:**

- **Определение вопросов:** Задание параметров, которые будут собраны у пользователя при инициализации проекта (например, имя проекта, автор, версия).
- **Настройка файлов и директорий:** Указание, какие файлы и папки должны быть включены или исключены из шаблона при копировании.
- **Управление переменными:** Определение переменных и их значений по умолчанию, которые будут заменены в файлах шаблона.
- **Пользовательские задачи (tasks):** Запуск скриптов (например, `pre-commit install`) после успешного копирования шаблона.

```{eval-rst}
.. toggle:: Показать

   .. literalinclude:: ../../copier.yaml
      :language: yaml
      :caption: copier.yaml
```

### 📋 .copier-answers.yml

После инициализации проекта из шаблона Copier создает в корне проекта файл `.copier-answers.yml`. Этот файл содержит *ответы*, предоставленные пользователем при запуске `copier copy` (или `copier update`), а также версию шаблона, из которой был скопирован проект.

**Важно:**
```{warning}
НИКОГДА не изменяйте файл `.copier-answers.yml` вручную. Это может привести к непредсказуемому поведению при последующих обновлениях шаблона. Подробнее см. [официальную документацию](https://copier.readthedocs.io/en/stable/updating/#never-change-the-answers-file-manually).
```

Этот файл необходим Copier для понимания, какая версия шаблона была использована и какие ответы были даны, чтобы умело применить изменения при обновлении.

## 🔧 Основные команды Copier

### Инициализация проекта

Команда `copier copy` используется для создания нового проекта из шаблона.

```bash
copier copy <путь_к_шаблону> <путь_к_целевой_директории>
```

*   `<путь_к_шаблону>`: может быть URL репозитория Git (например, `https://github.com/user/template-repo.git`) или локальным путем.
*   `<путь_к_целевой_директории>`: директория, куда будет скопирован шаблон. Если не существует, будет создана.

Copier задаст вопросы, определённые в `copier.yaml`, и сгенерирует проект, заменив шаблонные переменные на ответы пользователя.

### Обновление проекта

Команда `copier update` используется для обновления уже скопированного проекта до новой версии шаблона.

```bash
# В корне директории проекта
copier update
```

*   Copier автоматически определит версию шаблона, из которой был создан проект, и загрузит более новую версию (обычно последнюю по тегам Git).
*   Он сравнит старую и новую версии шаблона, а затем попытается умно применить изменения к вашему проекту, учитывая ваши предыдущие ответы.
*   Если в шаблоне были добавлены новые вопросы, Copier задаст их.

### Повторное копирование

Команда `copier recopy` используется для "грубого" обновления, когда стандартный `update` не справляется или привёл к сложным конфликтам.

```bash
copier recopy
```

*   Эта команда пересоздаёт проект из шаблона *с нуля*, используя сохранённые ответы из `.copier-answers.yml`.
*   Все изменения, внесённые вручную в файлы шаблона, будут *перезаписаны*.
*   После `recopy` можно использовать инструменты Git (например, `git diff`, `git merge-tool`) для ручного объединения изменений.

## 📝 Работа с конфликтами при обновлении

#TODO: добавить в пре-коммит хук для обработки этих случаев 

При обновлении (`copier update`) могут возникнуть конфликты, если шаблон изменил файл, который вы также редактировали в своём проекте.

*   Copier по умолчанию использует стратегию `--conflict=inline`, которая вставляет маркеры конфликта (например, `<<<<<<<`, `=======`, `>>>>>>>`) в файлы.
*   Также доступна стратегия `--conflict=rej`, которая сохраняет конфликты в отдельные файлы с расширением `.rej`.
*   После обновления, если были конфликты, необходимо **вручную** их разрешить и закоммитить результат.
*   Рекомендуется использовать инструменты контроля версий (например, `git status`, `git diff`) и, при необходимости, Git-хуки (например, `pre-commit`) для предотвращения коммита файлов с неразрешёнными конфликтами.

## 🔗 Полезные ссылки

- [Официальная документация Copier](https://copier.readthedocs.io/)