# Git

Git — это распределённая система контроля версий (VCS). Она позволяет отслеживать изменения в файлах, координировать работу нескольких разработчиков над одним проектом и управлять историей изменений кода. Git хранит проект как набор файлов в специальной директории (`.git`), создавая снимки (commits) состояния файлов в определённые моменты времени. Это позволяет легко возвращаться к предыдущим версиям, создавать независимые ветки разработки и объединять изменения.

## 🔧 Основные команды Git

#TODO: сделать, чтобы их можно было копировать

### Инициализация и настройка

*   `git init`: Инициализирует новый локальный репозиторий Git в текущей директории.
*   `git config --global user.name "Ваше Имя"`: Устанавливает имя пользователя для всех репозиториев на этом компьютере.
*   `git config --global user.email "ваш.email@example.com"`: Устанавливает email пользователя для всех репозиториев на этом компьютере.

### Работа с файлами и изменениями

*   `git status`: Показывает статус файлов в рабочей директории (какие изменены, добавлены, не отслеживаются).
*   `git add <файл>`: Добавляет указанный файл в индекс (staging area), готовя его к включению в следующий коммит.
*   `git add .`: Добавляет все *изменённые* и *новые* файлы в индекс.
*   `git diff`: Показывает различия между рабочей директорией и индексом (ещё не добавленные изменения).
*   `git diff --staged` (или `git diff --cached`): Показывает различия между индексом и последним коммитом (изменения, которые будут в следующем коммите).

### Работа с коммитами

*   `git commit -m "Краткое описание изменений"`: Создаёт новый коммит с изменениями из индекса и добавляет сообщение к коммиту.
*   `git log`: Показывает историю коммитов (автор, дата, сообщение).
*   `git log --oneline`: Показывает сокращённую историю коммитов (одна строка на коммит).

### Работа с ветками

*   `git branch`: Показывает список локальных веток и отмечает текущую.
*   `git branch <имя_ветки>`: Создаёт новую ветку с указанным именем.
*   `git checkout <имя_ветки>`: Переключается на указанную ветку.
*   `git switch <имя_ветки>`: Альтернатива `git checkout`, также переключается на ветку.
*   `git checkout -b <имя_новой_ветки>`: Создаёт новую ветку и сразу переключается на неё.
*   `git merge <имя_ветки>`: Объединяет изменения из указанной ветки в текущую ветку.

### Работа с удалённым репозиторием

*   `git remote add origin <URL_репозитория>`: Добавляет удалённый репозиторий (обычно GitHub) с именем `origin`.
*   `git fetch`: Загружает изменения из удалённого репозитория, но не объединяет их с локальной веткой.
*   `git pull`: Загружает и автоматически объединяет изменения из удалённого репозитория в текущую локальную ветку.
*   `git push`: Отправляет локальные коммиты в удалённый репозиторий.
*   `git push -u origin <имя_ветки>`: Отправляет ветку в удалённый репозиторий и устанавливает связь, чтобы в будущем можно было использовать просто `git push`.

## 📋 .gitignore

Файл `.gitignore` указывает Git, какие файлы и директории *игнорировать*, то есть *не отслеживать* и *не добавлять* в репозиторий. Это позволяет держать репозиторий чистым и избегать попадания туда ненужных или потенциально опасных данных.

```{tip} Обычно в .gitignore добавляют

**Файлы окружения:** `.env`, `*.env` — файлы с конфиденциальными данными (ключи, пароли).
**Виртуальные окружения Python:** `venv/`, `.venv/`, `env/` — директории с установленными пакетами.
**Кэш и временные файлы:** `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.coverage` — файлы, создаваемые интерпретатором или инструментами разработки.
**Логи:** `*.log`, `logs/` — файлы с записями событий выполнения.
**Системные файлы:** `.DS_Store` (macOS), `Thumbs.db` (Windows) — файлы, создаваемые операционной системой.
**Файлы редакторов:** `.vscode/`, `.idea/` — директории с настройками IDE (иногда включают, если хотят поделиться настройками, но часто исключают).
**Чувствительные файлы:** - ключи, пароли, токены
```

```{eval-rst}
.. toggle:: Показать

   .. literalinclude:: ../../.gitignore
      :language: text
      :caption: .gitignore
```

## 🔗 Полезные ссылки
[Официальный сайт Git](https://git-scm.com/)

[Подробная инструкция по установке и настройке Git](https://habr.com/ru/articles/541258/)

[Лучший видеокурс по работе с Git для новичков](https://www.youtube.com/playlist?list=PLDyvV36pndZFHXjXuwA_NywNrVQO0aQqb)

[Курс по Git и GitHub на Stepik](https://stepik.org/course/125248)

[Тренажёр по Git](https://learngitbranching.js.org/)